<Window x:Class="CMCS_WPF.Views.CoordinatorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Coordinator - Review Claims" Height="600" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Pending Claims" FontSize="18" FontWeight="Bold" Margin="0,0,0,10" />
        <DataGrid Grid.Row="1" ItemsSource="{Binding Claims}" AutoGenerateColumns="False" IsReadOnly="True" Name="ClaimsGrid" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Lecturer" Binding="{Binding LecturerName}" Width="150"/>
                <DataGridTextColumn Header="Date" Binding="{Binding DateSubmitted}" Width="140"/>
                <DataGridTextColumn Header="Hours" Binding="{Binding HoursWorked}" Width="80"/>
                <DataGridTextColumn Header="Rate" Binding="{Binding HourlyRate}" Width="90"/>
                <DataGridTextColumn Header="Total" Binding="{Binding TotalAmount}" Width="90"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
                <DataGridTemplateColumn Header="Actions" Width="180">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Approve" Margin="2" Command="{Binding DataContext.ApproveCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding Id}" Background="LightGreen"/>
                                <Button Content="Reject" Margin="2" Command="{Binding DataContext.RejectCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding Id}" Background="Salmon"/>
                                <Button Content="Details" Margin="2" Click="Details_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
